\section{Conclusions}

\begin{frame}{Conclusions \& Future Work}
  \begin{itemize}
    \item \textbf{Summary:} two-stream spatial+frequency fusion with multi-task learning \texttt{[TODO: state whether it beat single-branch/single-task baselines once you have numbers]}.
    \item The multi-task framing forced the network to disentangle post-processing artifacts from authenticity signal; the anti-shortcut fix stopped the frequency branch from just learning ``is this JPEG''.
    \item Uncertainty-weighted loss vs.\ manual $\alpha/\beta$: \texttt{[TODO: which worked better, from the ablation plot]}.
    \item \textbf{Limitations:} single dataset (RRDataset), CNN/ConvNeXt backbones only (no ViT/CLIP), generalization to unseen generators untested.
    \item \textbf{Future work:} cross-dataset generalization, transformer backbones, adversarial robustness, richer transform taxonomy.
  \end{itemize}
\end{frame}
